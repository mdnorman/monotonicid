{
  "name": "monotonicid",
  "version": "1.0.0",
  "description": "A web application for decoding timestamp-based monotonic identifiers",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "test": "pnpm --filter web test",
    "lint": "pnpm --filter web lint"
  },
  "keywords": [
    "monotonic",
    "timestamp",
    "decoder",
    "utility"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "20.19.9"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.14.2"
}
